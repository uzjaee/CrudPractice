spring:
  datasource:
    url: jdbc:mysql://localhost:3306/crudexercise
    username: mysql
    password: password
    driver: com.mysql.cj.jdbc.Driver
  mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: crudExercise
